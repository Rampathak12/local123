services:
  - type: web
    name: productjpa
    runtime: docker
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: productjpa-db
          property: connectionString

databases:
  - name: productjpa-db
    databaseName: productdb
    user: productjpa
